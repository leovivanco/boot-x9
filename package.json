{
  "name": "boot-x9",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "dev": "pnpm exec concurrently \"pnpm --filter frontend dev\" \"python3 -m uvicorn backend.server:app --reload --port 8000\"",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint"
  },
  "devDependencies": {
    "concurrently": "9.0.1"
  }
}
